name: Web Automation Workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
  
    steps:
    #download repo
    - uses: actions/checkout@v2

    #setup java
    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'temurin'
    #install browser
    - name: Setup Edge
      uses: browser-actions/setup-edge@v1.1.0
   #setup gradlew
    - name: Setup Gradlew
      run: chmod +x gradlew
    #Execute gradle command for running cucumber test
    - name: Execute Test
      run: gradlew.bat runCucumber
